<%= form_with(model: admin) do |form| %>
  <% if admin.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

      <ul>
        <% admin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :words, style: "display: block" %>
    <%= form.text_field :words %>
  </div>

  <div>
    <%= form.label :meanings, style: "display: block" %>
    <%= form.text_field :meanings %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
